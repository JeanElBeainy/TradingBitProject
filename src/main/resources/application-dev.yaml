spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tradingbit_db?createDatabaseIfNotExist=true
    username: root
    password: MyNewPass123!
  jpa:
    show-sql: true
websiteUrl: http://localhost:8080
server:
  servlet:
    session:
      timeout: 10m